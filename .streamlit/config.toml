[server]
maxUploadSize = 50
maxMessageSize = 50
# The two lines below fix "AxiosError 403" / silent upload failures on mobile browsers
# behind a proxy (Streamlit Community Cloud, Nginx, Cloudflare, ngrok).
enableXsrfProtection = false
enableCORS = false

[browser]
gatherUsageStats = false
